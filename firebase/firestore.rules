rules_version = '2';
// Saved workspaces. Anonymous Auth is enough for a small collaborative tool;
// any signed-in (incl. anonymous) user may read/write workspaces.
service cloud.firestore {
  match /databases/{database}/documents {
    match /workspaces/{id} {
      allow read, write: if request.auth != null;
    }
  }
}
