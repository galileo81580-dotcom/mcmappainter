{
  "rules": {
    "presence": {
      "$workspace": {
        ".read": "auth != null",
        "$uid": {
          ".write": "auth != null && auth.token.firebase.sign_in_provider === 'github.com' && auth.uid === $uid"
        }
      }
    }
  }
}
